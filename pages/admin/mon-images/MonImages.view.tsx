import { MonImage } from "../../../types";

export interface MonImagesProps {
  monImages: MonImage[];
}

const MonImages: React.FC<MonImagesProps> = ({ monImages }) => {
  return <></>;
};

export default MonImages;
