import { MonTier } from "../../types";

export interface CollectionStatusItemProps {
  tier: MonTier;
  value: number;
  max: number;
}

const CollectionStatusItem: React.FC<CollectionStatusItemProps> = ({ tier }) => {
  return <></>;
};

export default CollectionStatusItem;
